import { TemporizadorPipe } from './temporizador.pipe';

describe('TemporizadorPipe', () => {
  it('create an instance', () => {
    const pipe = new TemporizadorPipe();
    expect(pipe).toBeTruthy();
  });
});
